# Kotoba Studio — 环境变量样例
#
#   cp .env.example .env
#
# 每一项都对应一个 KOTOBA_ 前缀的环境变量；真实环境变量优先于本文件。
# 全部可省略，下面写的就是默认值。`.env` 已被 .gitignore 与 pre-commit 钩子
# 拦住，不会进仓库——但请仍然不要把密钥写进任何会被同步的目录。
#
# Every setting below maps to a KOTOBA_-prefixed environment variable, which
# always wins over this file. All of them are optional; the values shown are the
# defaults. `.env` is git-ignored and blocked by the pre-commit hook.

# ── 数据 ──────────────────────────────────────────────────────────────────
# 数据库、截图、音频、词典与备份的存放位置。
# 留空则用系统默认：macOS ~/Library/Application Support/KotobaStudio，
# Windows %APPDATA%\KotobaStudio。
# KOTOBA_DATA_DIR=./data

# ── 服务 ──────────────────────────────────────────────────────────────────
# 监听地址。要用手机扫码访问，必须改成 0.0.0.0 并确保在同一局域网。
# KOTOBA_HOST=127.0.0.1
# KOTOBA_PORT=8720

# 前端构建产物的位置。留空则用仓库里的 frontend/dist。
# KOTOBA_WEB_DIST=

# 开发时 Vite 的地址，用于 CORS。生产（单端口托管）用不到。
# 值是 JSON 数组。
# KOTOBA_CORS_ORIGINS=["http://localhost:5174","http://127.0.0.1:5174"]

# ── AI 解释（可选） ───────────────────────────────────────────────────────
# 收藏、查词和复习都不依赖 AI；不填这一项，应用照常工作。
#
# 优先推荐在 设置 → AI 解释 里填写，密钥会存进系统钥匙串（macOS Keychain /
# Windows 凭据管理器），不会落到磁盘上的明文文件里。
# 这里的变量是给无头部署与 CI 用的后备。
#
# DeepSeek 控制台：https://platform.deepseek.com/api_keys
# KOTOBA_AI_KEY=<在控制台生成的密钥，不要提交到任何仓库>
#
# 服务商、地址与模型在 设置 页里改，也可以直接改数据库里的 settings 表。
# 默认：deepseek · https://api.deepseek.com · deepseek-flash
