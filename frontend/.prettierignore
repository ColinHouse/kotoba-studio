# A .prettierignore switches Prettier off .gitignore, so the build output and
# the lock file that used to be skipped through it have to be named here.
dist/
dev-dist/
node_modules/
test-results/
playwright-report/
package-lock.json
