# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
/dist/

# Node
node_modules/
frontend/dist/
frontend/dev-dist/
*.tsbuildinfo

# App data (never commit user data, media, dictionaries or backups)
/data/
*.db
*.db-wal
*.db-shm
*.apkg
*.zip

# Local-only working material: research, design briefs, prompts, agent plans.
# Anything that is written *to* a model rather than *for* a reader lives here.
/private/

# Agent and editor tooling configuration is personal, not project state.
# How you drive an agent is your setup; what the project expects from one is AGENTS.md.
.claude/
.cursor/
.aider*
.windsurf*
.continue/
.opencode/
opencode.json
opencode.jsonc
.codex/
.goose/
.gemini/
.mcp.json

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log
.env
.env.*
!.env.example

test-results/
playwright-report/

# Hypothesis keeps its example database next to the tests; it is a cache, not code.
.hypothesis/
